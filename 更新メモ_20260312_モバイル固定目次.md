# 更新メモ（2026-03-12 / モバイル固定目次）

## 対象
- `index.html`

## 変更内容
- スマホ表示時に、画面下へ固定されるリッチメニュー型の目次を追加
  - 7セクションへのアンカーを横スクロール可能なチップで配置
  - サイト既存の配色（`--navy`, `--blue`, `--blue-light`）に合わせたカード調デザイン
- 既存の上部固定目次（`toc-section`）はスマホ時のみ非表示化
- 現在閲覧中セクションに応じて、下部メニューの該当項目が`active`表示されるようJS連動を追加
- 下部固定メニューとコンテンツ重なりを避けるため、スマホ時の`body`下余白を追加

## 期待効果
- スマホで常に目次操作が可能になり、セクション移動が短時間でできる
- ページ全体の導線が明確になり、長文コンテンツの回遊性が向上する
