# 更新メモ（2026-03-12 / Android Chrome表示対策）

## 対象
- `index.html`

## 修正内容
- Android Chromeでハンバーガーが出ないケース対策として、表示条件を端末判定依存から変更
  - `.hamburger-fab` を常時表示に変更（`display:flex`）
  - `z-index` を `2200` に引き上げ、重なりで見えないリスクを低減
- 1024px以下ではヘッダー右余白（`padding-right:72px`）を維持

## 目的
- iPhone/Android Chrome双方で、ヘッダー右側のハンバーガーメニューを確実に表示する
