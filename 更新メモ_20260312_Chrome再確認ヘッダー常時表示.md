# 更新メモ（2026-03-12 / Chrome再確認 ヘッダー常時表示）

## 対象
- `index.html`

## 再修正内容
- ハンバーガー表示を端末条件依存から完全に除外
  - ヘッダー右側に常時表示する通常ボタン化
  - `position:absolute` を廃止し、`nav-inner` 内フロー配置に変更
- ヘッダーレイアウト調整
  - `nav-inner` を `justify-content:flex-start`
  - `nav-links` に `margin-left:auto` を付与して右寄せ
  - ハンバーガーは `nav-links` の右に固定される構成へ
- 1024px以下は `nav-links` 非表示で、ハンバーガーのみ表示される構成を維持

## 目的
- Android Chromeで表示判定や位置ずれの影響を受けず、ヘッダー右側で確実にハンバーガーを表示する
