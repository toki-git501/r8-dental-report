# 更新メモ（2026-03-12 / Androidヘッダー固定再修正）

## 対象
- `index.html`

## 修正内容
- ハンバーガーメニューをページ固定配置から「ヘッダー内部配置」に変更
  - `nav-inner` を `position: relative`
  - ハンバーガーを `nav-inner` 内へ移動して `position: absolute; right:12px; top:8px;`
- Androidで右へスクロールしないと表示されない問題への対策
  - `html { overflow-x: clip }`
  - `body { overflow-x: hidden }`
  - モバイル時に `nav-inner` へ右側余白を追加してタイトルとボタン重なりを回避

## 期待効果
- iPhone/Androidともに、初期表示時点でヘッダー右側にハンバーガーメニューが表示される
